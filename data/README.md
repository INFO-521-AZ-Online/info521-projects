# Data

## `nhanes_2021_2022.csv` — deployed default (real data)

The dataset `info521.data.load_clinical()` loads by default. A curated extract of
**NHANES 2021–2022** (CDC, cycle suffix `_L`): 5,102 adults aged 18–79, complete
cases on all columns. Built by `build_nhanes.py`, which downloads six public
NHANES XPT files, merges them on `SEQN`, and derives oscillometric blood pressure.

Columns, in order: `age, bmi, waist, chol, hdl, hba1c, sbp, dbp`.

- **Target** (`sbp`): systolic blood pressure (mmHg), the continuous Part 1
  outcome.
- **Reserved** (`dbp`): diastolic blood pressure. Used *only* to build the
  Part 2 hypertension label (ACC/AHA: SBP ≥ 130 or DBP ≥ 80) via
  `info521.data.hypertension(ds)`. It is excluded from the feature matrix and is
  never the target, so the hypertension label can't leak into its own predictors.
- **Features**: `age, bmi, waist, chol, hdl, hba1c`.

Survey weights are deliberately ignored — this is an algorithms course about
estimators, not population inference.

Rebuild (downloads are cached in the gitignored `raw/`):

```bash
python build_nhanes.py
```

## `clinical_reference.csv` — synthetic offline fallback (contract mirror)

A **synthetic** reference dataset (2,000 patients) generated by
`generate_reference_data.py` (seed 521) so the pipeline runs reproducibly with no
network access. **This is not real clinical data.**

It is a **contract-faithful mirror** of the deployed NHANES dataset: the same
eight columns in the same order (`age, bmi, waist, chol, hdl, hba1c, sbp, dbp`),
`sbp` as the target, `dbp` reserved — so `load_clinical(path=...)`, `binarize`,
and `hypertension(ds)` all behave identically on it, at a matched hypertension
prevalence (~0.388). It is built as a Gaussian copula over the NHANES correlation
matrix with NHANES-matched marginals. The one intentional departure is
demographic, not contractual: `age` is **young-skewed** (the real NHANES adult
distribution is roughly flat) so the sparse-upper-tail lesson — predictive
uncertainty widening where data is thin (milestone 1.2) stays visible.

Load it explicitly when offline:

```python
info521.data.load_clinical(path="data/clinical_reference.csv")
```

Regenerate (prints a QA block and self-asserts the contract + targets):

```bash
python generate_reference_data.py
```

## Column contract (honored by `info521.data.load_clinical`)

Header row; a continuous target column named `sbp`; a `dbp` column, if present, is
reserved (excluded from features and surfaced separately for the hypertension
label); an `age` column serving as the Part 1 primary predictor. See
`../instructor/dataset-curation.md`.
